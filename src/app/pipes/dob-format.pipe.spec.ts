import { DobFormatPipe } from './dob-format.pipe';

describe('DobFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DobFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
